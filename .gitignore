# python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/

# tooling caches
.ruff_cache/
.pytest_cache/

# credentials -- save-aws-creds.sh refuses to write here unless this is listed
.aws_creds

# local claude settings
.claude/settings.local.json
CLAUDE.local.md

# scan output -- the Makefile's defaults (JSON/SCRIPT/HTML) and the dated
# names a hand-run scan writes
findings.json
cleanup.sh
remediation.sh
report.html
scan-*.json
cleanup-*.sh
zombiescan-report.html

# scraped AWS Builder Center pages -- local reference only, not ours to republish
HACKATHON.md
RULES.md
